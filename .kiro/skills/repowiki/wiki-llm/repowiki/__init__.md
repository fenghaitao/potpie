# repowiki

The top-level package for repowiki. Exposes the package version and makes the `repowiki` namespace importable.

**Source:** `repowiki/__init__.py`

## Types

**`__version__`** — Current release version of the package (`'0.1.0'`).
