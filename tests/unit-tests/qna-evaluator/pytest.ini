[pytest]
# Isolate this test directory from the root tests/conftest.py which requires
# a live Postgres/Redis connection.  By placing pytest.ini here, pytest treats
# this directory as its own rootdir and does NOT walk up to tests/conftest.py.
asyncio_mode = auto
