[pytest]
testpaths = .
asyncio_mode = auto
# Don't walk up to the repo root conftest (which imports app.main)
markers =
    integration: live end-to-end tests that require external services (e.g. Copilot CLI)
