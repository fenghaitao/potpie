[pytest]
testpaths = .
asyncio_mode = auto
pythonpath = ..
# Add the repository root to PYTHONPATH so these unit tests can import the application package
markers =
    integration: live end-to-end tests that require external services (e.g. Copilot CLI)
